import React from 'react';

// Header component
const ScrollHeader = ({ text }) => {
    return (
        <div className="header">
            {text}
        </div>
    );
};

export default ScrollHeader;